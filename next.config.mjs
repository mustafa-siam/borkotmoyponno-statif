/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "i.ibb.co",
      "i.ibb.com",
      "example.com",
      "th.bing.com",
    ],
  },
  output: "standalone",
};

export default nextConfig;
